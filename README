### 目前模型是用字典存储的，key支持string
### 修改ftrl.h中的初始化函数，可以用vector来存储模型，速度会更快。
example:
make
cat train.txt |./ftrl_train -core 8 -m model.txt (8 thread)
cat test.txt | ./ftrl_predict model.txt 4 result.txt  (4 thread)
cat result.txt |./AUC.py 

